apiVersion: apps/v1
kind: Deployment
metadata:
  name: znaphub-redis
  namespace: znaphub
spec:
  replicas: 1
  selector:
    matchLabels:
      app: znaphub-redis
  template:
    metadata:
      labels:
        app: znaphub-redis
    spec:
      containers:
        - name: redis
          image: redis:7-alpine
          ports:
            - containerPort: 6379
          resources:
            requests:
              memory: "128Mi"
              cpu: "100m"
            limits:
              memory: "256Mi"
              cpu: "200m"
---
apiVersion: v1
kind: Service
metadata:
  name: znaphub-redis
  namespace: znaphub
spec:
  selector:
    app: znaphub-redis
  ports:
    - port: 6379
      targetPort: 6379
