apiVersion: v1
kind: ConfigMap
metadata:
  name: eventflow-config
  namespace: eventflow
data:
  appsettings.json: |
    {
      "ConnectionStrings": {
        "EventFlowContext": "Host=postgres.eventflow.svc.cluster.local;Port=5432;Database=EventFlow;Username=postgres;Password=postgrespw;Timeout=300;CommandTimeout=300"
      },
      "Logging": {
        "LogLevel": {
          "Default": "Information",
          "Microsoft.AspNetCore": "Warning"
        }
      },
      "AllowedHosts": "*",
      "Minio": {
        "Endpoint": "minio.eventflow.svc.cluster.local:9000",
        "Secure": false,
        "Bucket": "eventphotos"
      }
    }
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: eventflow-api
  namespace: eventflow
spec:
  replicas: 2
  selector:
    matchLabels:
      app: eventflow-api
  template:
    metadata:
      labels:
        app: eventflow-api
    spec:
      containers:
        - name: eventflow-api
          image: awingray/eventflow-api:latest
          ports:
            - containerPort: 8080
            - containerPort: 8081
          env:
            - name: ASPNETCORE_ENVIRONMENT
              value: "Production"
          volumeMounts:
            - name: app-config
              mountPath: /app/appsettings.json
              subPath: appsettings.json
          livenessProbe:
            httpGet:
              path: /health/live
              port: 8080
            initialDelaySeconds: 30
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /health/ready
              port: 8080
            initialDelaySeconds: 5
            periodSeconds: 5
          startupProbe:
            httpGet:
              path: /health/startup
              port: 8080
            failureThreshold: 30
            periodSeconds: 10
      volumes:
        - name: app-config
          configMap:
            name: eventflow-config
            items:
              - key: appsettings.json
                path: appsettings.json
---
apiVersion: v1
kind: Service
metadata:
  name: eventflow-api
  namespace: eventflow
spec:
  selector:
    app: eventflow-api
  ports:
    - name: http
      port: 80
      targetPort: 8080
    - name: metrics
      port: 8081
      targetPort: 8081
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: eventflow-ingress
  namespace: eventflow
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /
spec:
  rules:
    - host: grayzl.net
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: eventflow-api
                port:
                  number: 80